development:
  sessions:
    default:
      database: os_dev
      hosts:
        - localhost:27017
      options:
  options:
    allow_dynamic_fields: true
    raise_not_found_error: false
production:
  sessions:
    default:
      database: os_dev
      hosts:
        - localhost:27017
      options:
  options:
    allow_dynamic_fields: true
    raise_not_found_error: false
test:
  sessions:
    default:
      database: os_test
      hosts:
        - localhost:27017
      options:
        consistency: :strong
        max_retries: 1
        retry_interval: 0
